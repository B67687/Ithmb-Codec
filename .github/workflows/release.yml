# release.yml — Cross-compile enterprise release pipeline
name: release
on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
      - run: cargo test --workspace

  build:
    name: Build ${{ matrix.target }}
    needs: [test]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
          - target: x86_64-apple-darwin
            os: macos-15-intel
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          cache: true
          cache-key: "release-${{ matrix.target }}-${{ hashFiles('rust-toolchain.toml') }}"
          targets: ${{ matrix.target }}
      - name: Build ithmb-cli
        run: cargo build -p ithmb-cli --release --target ${{ matrix.target }}
      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: tar czf ithmb-cli-${{ matrix.target }}.tar.gz -C target/${{ matrix.target }}/release ithmb
      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: 7z a ithmb-cli-${{ matrix.target }}.zip ./target/${{ matrix.target }}/release/ithmb.exe
      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ithmb-cli-${{ matrix.target }}
          path: |
            ithmb-cli-${{ matrix.target }}.tar.gz
            ithmb-cli-${{ matrix.target }}.zip
          if-no-files-found: warn

  python-wheels:
    name: Build wheel (${{ matrix.target }})
    needs: [test]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
          - target: x86_64-apple-darwin
            os: macos-15-intel
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - name: Install maturin
        run: pip install maturin==1.14.1
      - name: Build wheel
        run: maturin build --release --manifest-path pymod/Cargo.toml --target ${{ matrix.target }}
      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ithmb-python-${{ matrix.target }}
          path: target/wheels/*.whl
          if-no-files-found: error
  release:
    name: Create Release
    needs: [build, python-wheels]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: artifacts
          pattern: ithmb-*
          merge-multiple: true
      - name: Create RELEASE_NOTES.md
        run: |
          cat > RELEASE_NOTES.md << 'EOF'
          # Ithmb-Codec Enterprise Release

          This is an **Enterprise** cross-compiled release of Ithmb-Codec.

          Built for:
          - Linux x86_64
          - Linux ARM64 (aarch64)
          - macOS x86_64 (Intel)
          - macOS ARM64 (Apple Silicon)
          - Windows x86_64

          Plus **Python wheels** for all platforms (`pip install ithmb-python`).

          All binaries are built with SIMD acceleration enabled.

          ## Installation

          Extract the archive for your platform and run the `ithmb` binary:

          ```bash
          # Linux / macOS
          tar xzf ithmb-cli-<target>.tar.gz
          ./ithmb --help

          # Windows
          # Extract ithmb-cli-<target>.zip and run ithmb.exe
          ```
          EOF
      - name: Generate changelog
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5 # v4
        with:
          config: cliff.toml
        env:
          OUTPUT: CHANGELOG.md
      - name: Combine release notes
        run: |
          cat RELEASE_NOTES.md > BODY.md
          echo "" >> BODY.md
          echo "---" >> BODY.md
          echo "" >> BODY.md
          cat CHANGELOG.md >> BODY.md
      - name: Create GitHub Release
        uses: softprops/action-gh-release@c12583777ecdfd3be55c69cf75464299dc01057e # v3
        with:
          body_path: BODY.md
          generate_release_notes: true
          files: artifacts/*
